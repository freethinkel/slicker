themes {
    slicker {
        fg "{{ foreground }}"
        bg "{{ background }}"
        black "{{ color0 }}"
        red "{{ color1 }}"
        green "{{ color2 }}"
        yellow "{{ color3 }}"
        blue "{{ accent }}"
        magenta "{{ color5 }}"
        cyan "{{ color6 }}"
        white "{{ color7 }}"
        orange "{{ color3 }}"
    }
}
